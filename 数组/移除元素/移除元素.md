# leetcode 27 移除元素
  数组nums
  移除val值，返回剩余的长度
  ## 1. 自己的思路
    使用两个下标指针i和j，i用来遍历完数组nums，初始i=0,j=0;当遍历到nums[i]，若nums[i]!=val,选择nums[j] = nums[i]，直接将原来的值覆盖掉,j++;若nums[i]==val，则不进行任何操作。这样的话，数组nums中不等于val的元素有j个，且0~j-1为不等于val的元素。
    
